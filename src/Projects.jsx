import React from 'react'
import{ArrowRight,Cpu}from'lucide-react'
import {guidedProject,roleGroups} from './data/projects'

export default function Projects({onStartRiscv}){return <div className="projects-page"><div className="page-head"><div className="eyebrow">PORTFOLIO PROJECTS</div><h1>Hardware design projects</h1><p>Build substantial RTL, verification, FPGA and architecture projects that give you concrete work to discuss in applications and interviews.</p></div><section className="guided-project"><div className="guided-project-icon"><Cpu size={28}/></div><div className="guided-project-copy"><div><span className="project-level">GUIDED · {guidedProject.level.toUpperCase()}</span><span className="guided-time">8 stages · ~2.5 hours</span></div><h2>{guidedProject.title}</h2><p>{guidedProject.build}</p><div className="project-tags">{guidedProject.skills.slice(0,4).map(skill=><span key={skill}>{skill}</span>)}</div></div><button className="guided-start" onClick={onStartRiscv}>Start building <ArrowRight size={16}/></button></section>{roleGroups.map(group=><section className="project-role" key={group.role}><div className="project-role-head"><h2>{group.role}</h2><p>{group.summary}</p></div><div className="project-grid">{group.projects.map(project=><article className="project-card" key={project.slug}><div className="project-card-top"><span className="project-level">{project.level}</span><h3>{project.title}</h3></div><p className="project-build"><strong>Build:</strong> {project.build}</p><div><strong>Key skills</strong><div className="project-tags">{project.skills.map(skill=><span key={skill}>{skill}</span>)}</div></div><div><strong>Suggested deliverables</strong><ul>{project.deliverables.map(item=><li key={item}>{item}</li>)}</ul></div><p className="project-why"><strong>Why it helps:</strong> {project.why}</p><a href={`/projects/${project.slug}/`}>Project guide</a></article>)}</div></section>)}</div>}
