# HDLForge problem URLs

The pages in this directory are crawlable entry points for individual HDLForge practice problems. Each SEO page should link users into the interactive application using the corresponding problem ID.

Problem IDs are maintained in `src/seoProblemRoutes.js`.
